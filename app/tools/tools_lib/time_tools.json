[
  {
    "type": "function",
    "function": {
      "name": "time.current_datetime",
      "description": "Returns the current date and time (ISO 8601). Use this to resolve relative dates like 'today' before calling calendar tools.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  }
]